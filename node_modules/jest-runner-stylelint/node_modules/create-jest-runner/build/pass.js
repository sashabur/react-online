'use strict';

var toTestResult = require('./toTestResult');

var pass = function pass(_ref) {
  var start = _ref.start,
      end = _ref.end,
      test = _ref.test;
  return toTestResult({
    stats: {
      failures: 0,
      pending: 0,
      passes: 1,
      start,
      end
    },
    tests: [Object.assign({ duration: end - start }, test)],
    jestTestPath: test.path
  });
};

module.exports = pass;